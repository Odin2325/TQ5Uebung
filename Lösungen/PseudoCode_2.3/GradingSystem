# Grading System

START
    INPUT score

    IF score >= 90
        grade = "A"
    ELSE IF score >= 80
        grade = "B"
    ELSE IF score >= 70
        grade = "C"
    ELSE IF score >= 60
        grade = "D"
    ELSE
        grade = "F"

    PRINT "Grade: ", grade
END
